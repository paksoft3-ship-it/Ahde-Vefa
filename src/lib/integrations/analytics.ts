/**
 * Analytics adapter (PLACEHOLDER).
 * Wire up an analytics provider (e.g. Plausible, GA4) later.
 * Keep it privacy-respecting; do not track sensitive donor data.
 */

export function trackEvent(_name: string, _props?: Record<string, unknown>) {
  // TODO: forward to a privacy-friendly analytics provider.
}
